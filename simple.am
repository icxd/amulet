fn strlen(s: c_char*) -> usz {
  mut i = 0 as usz;
  while s[i] != 0 as c_char {
    i = i + 1;
  }
  return i;
}

fn main(
  argc: i32,
  argv: c_char**,
) {
  let executable: c_char* = argv[0];
  let executable_len = strlen(executable);
}
